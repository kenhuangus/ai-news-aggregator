---
phase: 05-documentation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/llm_client.py
  - generators/hero_generator.py
  - CLAUDE.md
  - LICENSE
autonomous: true

must_haves:
  truths:
    - "grep -ri 'rdsec' agents/ generators/ returns no results"
    - "grep -r '@trendmicro.com' . returns no results"
    - "LICENSE file contains Apache 2.0 text"
    - "Copyright line references AATF, not Trend Micro"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 license text"
      contains: "Apache License"
    - path: "agents/llm_client.py"
      provides: "LLM client without RDSec references"
    - path: "generators/hero_generator.py"
      provides: "Hero generator without RDSec/Trend Micro references"
    - path: "CLAUDE.md"
      provides: "Documentation with generic provider references"
  key_links: []
---

<objective>
Remove all RDSec and Trend Micro references from Python source code and documentation, and replace the LICENSE file with Apache 2.0.

Purpose: Clean the codebase of internal references so it can be released publicly without exposing internal infrastructure details.
Output: Cleaned Python files, updated CLAUDE.md, and Apache 2.0 LICENSE file.
</objective>

<execution_context>
@/Users/ryand/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryand/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-cleanup/05-RESEARCH.md

Key cleanup targets from research:
- agents/llm_client.py line 39: "# RDSec proxy model limit" → "# Model max token limit"
- generators/hero_generator.py lines 94, 117: RDSec endpoints (remove deprecated paths)
- generators/hero_generator.py line 257: "No Trend Micro logos" → "No company logos"
- CLAUDE.md line 87: "via RDSec" → "via Gemini API"
- CLAUDE.md line 250: "via RDSec endpoint" → "via configured provider"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean Python source files</name>
  <files>agents/llm_client.py, generators/hero_generator.py</files>
  <action>
    In agents/llm_client.py:
    - Find the comment "# RDSec proxy model limit" (around line 39)
    - Change to "# Model max token limit"

    In generators/hero_generator.py:
    - Find any hardcoded RDSec endpoint URLs (lines ~94, ~117) - these are in deprecated/legacy code paths
    - Remove the entire deprecated code blocks that reference api.rdsec.trendmicro.com
    - Find "No Trend Micro logos" comment in the prompt (line ~257)
    - Change to "No company logos"
    - Remove any env var fallbacks that reference RDSec
  </action>
  <verify>
    grep -ri "rdsec" agents/ generators/ returns no results
    grep -ri "trend.micro\|trendmicro" agents/ generators/ returns no results (except TrendAI/AATF branding which is OK)
  </verify>
  <done>Python source files contain no RDSec or Trend Micro references</done>
</task>

<task type="auto">
  <name>Task 2: Clean CLAUDE.md documentation</name>
  <files>CLAUDE.md</files>
  <action>
    Search CLAUDE.md for:
    - "via RDSec" → change to "via Gemini API"
    - "via RDSec endpoint" → change to "via configured provider"
    - "RDSec API" → change to appropriate generic term
    - Any other RDSec or Trend Micro references (except TrendAI/AATF which should stay)

    Keep all references to:
    - TrendAI (project identity)
    - AATF (team identity)
    - Trend Red color references (branding)
  </action>
  <verify>
    grep -i "rdsec" CLAUDE.md returns no results
    grep -i "@trendmicro.com" CLAUDE.md returns no results
    TrendAI and AATF references remain intact
  </verify>
  <done>CLAUDE.md contains no internal infrastructure references</done>
</task>

<task type="auto">
  <name>Task 3: Replace LICENSE with Apache 2.0</name>
  <files>LICENSE</files>
  <action>
    Replace the entire LICENSE file content with standard Apache 2.0 license text.

    Copyright line should read:
    "Copyright 2026 AI Acceleration Task Force (AATF)"

    Do NOT include "Trend Micro" in the copyright line.

    Use the standard Apache 2.0 template from https://www.apache.org/licenses/LICENSE-2.0.txt
  </action>
  <verify>
    LICENSE file exists and starts with "Apache License"
    grep "Trend Micro" LICENSE returns no results
    grep "AATF" LICENSE returns the copyright line
  </verify>
  <done>LICENSE file contains Apache 2.0 text with AATF copyright</done>
</task>

</tasks>

<verification>
After all tasks complete, run these verification commands:

```bash
# No RDSec references in code
grep -ri "rdsec" --include="*.py" . && echo "FAIL: RDSec found" || echo "PASS: No RDSec"

# No Trend Micro references (except acceptable TrendAI/AATF)
grep -ri "trend.micro\|trendmicro\|@trendmicro" --include="*.py" --include="*.md" . && echo "FAIL: Trend Micro found" || echo "PASS: No Trend Micro"

# LICENSE is Apache 2.0
head -1 LICENSE | grep -q "Apache" && echo "PASS: Apache license" || echo "FAIL: Wrong license"
```
</verification>

<success_criteria>
- All grep verification commands pass (no RDSec, no Trend Micro refs)
- LICENSE file contains Apache 2.0 text
- TrendAI and AATF references preserved (project identity)
- Python files still syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-cleanup/05-01-SUMMARY.md`
</output>
